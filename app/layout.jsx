import "./globals.css"

export const metadata = {
    title: "Insta generator",
    description: "Insta Generator",
}

export default ({ children }) => (
    <html lang="en">
        <body>{children}</body>
    </html>
)